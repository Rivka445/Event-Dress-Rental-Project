version: '3.8'
services:
  redis:
    image: redis:7-alpine
    container_name: eventdress_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

volumes:
  redisdata:
